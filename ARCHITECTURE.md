# როგორ მუშაობს ეს API

დოკუმენტი ხსნის, **რა ხდება მოთხოვნის გაგზავნიდან პასუხის მიღებამდე** —
როგორც ინფრასტრუქტურის, ისე კოდის დონეზე.

---

## 1. ოთხი სერვისი

```
   ბრაუზერი (ფრონტენდი)
        │
        │  fetch('https://shop-api-kbe6.onrender.com/api/auth/login')
        ▼
   ┌─────────────────────────────────┐
   │  RENDER                         │   Node.js პროცესი (Express)
   │  shop-api                       │   უსმენს HTTP-ს, ამუშავებს ლოგიკას
   └────────────┬────────────────────┘
                │  Prisma → SQL (TLS)
                ▼
   ┌─────────────────────────────────┐
   │  NEON                           │   PostgreSQL, Frankfurt
   │  neondb                         │   ინახავს User და PasswordResetCode
   └─────────────────────────────────┘

   GITHUB ──── push ────► Render ხელახლა დეპლოის
   UPTIMEROBOT ── 5 წთ ──► /api/health  (რომ Render არ დაიძინოს)
```

| სერვისი | პასუხისმგებლობა |
|---|---|
| **GitHub** | კოდის შენახვა. Render აქედან იღებს წყაროს |
| **Render** | კოდის გაშვება. აქ ცხოვრობს Node-ის პროცესი |
| **Neon** | მონაცემების შენახვა. ცალკე სერვერია, ქსელით მიღწევადი |
| **UptimeRobot** | „კაკუნი" ყოველ 5 წუთში, რომ უფასო Render არ დაიძინოს |

**რატომ არის ბაზა ცალკე:** Render-ის უფასო კონტეინერს მუდმივი დისკი არ აქვს —
ყოველ გადატვირთვაზე ფაილები იშლება. ამიტომ მონაცემები ცალკე სერვისში ცხოვრობს.

---

## 2. ფაილების სტრუქტურა

```
shop-api/
├── src/
│   ├── server.js          # შესვლის წერტილი: middleware, როუტები, Swagger
│   ├── db.js              # Prisma-ს კლიენტის ერთი ინსტანცია
│   ├── middleware.js      # latency, validate(), requireAuth()
│   └── routes/
│       └── auth.js        # ყველა /api/auth/* ენდპოინტი
├── prisma/
│   ├── schema.prisma          # მონაცემთა მოდელი — SQLite (ლოკალური)
│   └── schema.postgres.prisma # იგივე მოდელი — Postgres (დეპლოი)
├── openapi.yaml           # API-ის აღწერა. Swagger UI აქედან იხატება
└── package.json
```

---

## 3. რა ხდება ერთი მოთხოვნისას

მაგალითი: `POST /api/auth/login`

```
┌─ 1. server.js ─────────────────────────────────────────────┐
│  cors()            ნებადართულია თუ არა ეს დომენი?          │
│  express.json()    სხეული JSON-ად წაიკითხე → req.body      │
│  latency()         ხელოვნური დაყოვნება (ARTIFICIAL_LATENCY)│
└────────────────────────┬───────────────────────────────────┘
                         ▼
┌─ 2. routes/auth.js ────────────────────────────────────────┐
│  validate(loginSchema)                                     │
│     Zod ამოწმებს: email არის? ფორმატი სწორია? password?    │
│     ❌ → 422 { message, code, errors: { email: "..." } }   │
│     ✅ → next()                                            │
└────────────────────────┬───────────────────────────────────┘
                         ▼
┌─ 3. handler ───────────────────────────────────────────────┐
│  prisma.user.findUnique({ where: { email } })              │
│  bcrypt.compare(პაროლი, user.passwordHash)                 │
│     ❌ → 401 INVALID_CREDENTIALS                           │
│     ✅ → jwt.sign(...) → res.json({ user, accessToken })   │
└────────────────────────────────────────────────────────────┘
```

**მთავარი პრინციპი:** თითო ფენა ერთ საქმეს აკეთებს და შემდეგს გადასცემს.
ვალიდაცია ერთ ადგილას წერია (`middleware.js`), ამიტომ **ყველა `422` ერთნაირია**.

---

## 4. როგორ ინახება პაროლი

პაროლი ბაზაში **არასდროს იწერება ღიად**.

```
რეგისტრაცია:
  "Password1"  ──bcrypt.hash(10 რაუნდი)──►  "$2a$10$N9qo8uLOickgx2ZMRZo..."
                                             ↑ ეს იწერება ბაზაში

შესვლა:
  "Password1"  ──bcrypt.compare(hash)──►  true / false
```

bcrypt **ცალმხრივია** — ჰეშიდან პაროლის აღდგენა შეუძლებელია. ბაზის მოპარვის
შემთხვევაშიც პაროლები დაცულია.

`10 რაუნდი` ნიშნავს, რომ გამოთვლა განზრახ ნელია (~100ms) — ეს brute-force-ს ძვირს ხდის.

---

## 5. როგორ მუშაობს ტოკენი (JWT)

### რა არის ტოკენი

სამი ნაწილი, წერტილებით გაყოფილი:

```
eyJhbGciOiJIUzI1NiJ9 . eyJzdWIiOiJjbXVja2ZneXUiLCJ0eXAiOiJhY2Nlc3MiLCJleHAiOjE3OTB9 . iltUNhCMKeUE
└──── header ────┘   └──────────────── payload ────────────────┘   └── signature ──┘
    ალგორითმი              { sub: userId, typ: "access", exp }         ხელმოწერა
```

**payload დაშიფრული არ არის** — ის უბრალოდ base64-ია და ნებისმიერს წაუკითხავს.
დაცულია **ხელმოწერა**: ის იქმნება `JWT_SECRET`-ით, რომელიც მხოლოდ სერვერმა იცის.

### რატომ ვერ გააყალბებ

```
თუ მომხმარებელი payload-ს შეცვლის (sub → სხვისი id),
ხელმოწერა აღარ დაემთხვევა → jwt.verify() შეცდომას აგდებს → 401
```

### რატომ არ ინახავს სერვერი სესიებს

```
requireAuth(req, res, next):
   1. წაიკითხე ჰედერი:  Authorization: Bearer eyJhbGci...
   2. jwt.verify(token, JWT_SECRET)
        ❌ ვადა გავიდა   → 401 TOKEN_EXPIRED
        ❌ ხელმოწერა ცუდი → 401 INVALID_TOKEN
        ✅ → req.userId = payload.sub → next()
```

სერვერს **არაფერი აქვს დასამახსოვრებელი** — ხელმოწერის შემოწმება საკმარისია.
ამიტომ `POST /auth/logout` მხოლოდ `204`-ს აბრუნებს: **ტოკენს კლიენტი შლის**.

ტოკენის ვადა 30 წუთია (`ACCESS_TOKEN_TTL`). ვადის შემდეგ ფრონტენდმა უნდა
დაიჭიროს `401` + `code: "TOKEN_EXPIRED"` და გადაამისამართოს `/login`-ზე.

---

## 6. პაროლის აღდგენის ფლოუ

```
┌─ ნაბიჯი 1 ─ POST /auth/forgot-password  { email } ─────────────┐
│                                                                 │
│  მომხმარებელი მოიძებნა?                                         │
│    არა  → უბრალოდ დააბრუნე 200 (არაფერი გააკეთო)               │
│    კი   → შექმენი 6-ციფრიანი კოდი: "302919"                     │
│           წაშალე ძველი გამოუყენებელი კოდები                     │
│           შეინახე bcrypt-ჰეშად + expiresAt = ახლა + 15 წუთი     │
│           დაბეჭდე კონსოლში + დააბრუნე devCode (სასწავლო რეჟიმი) │
│                                                                 │
│  ორივე შემთხვევაში პასუხი ერთნაირია:                            │
│  "If an account exists for this email, we have sent a code."     │
└─────────────────────────────────────────────────────────────────┘
```

> 🔑 **რატომ ერთნაირი პასუხი:** თუ „ასეთი მომხმარებელი არ არსებობს" დააბრუნებ,
> ფორმა იქცევა ინსტრუმენტად, რომლითაც შეიძლება გაარკვიო, **ვის აქვს ანგარიში**
> შენს საიტზე. ეს არის *account enumeration*. იგივე მიზეზით `/auth/login`
> აბრუნებს „არასწორი ელფოსტა ან პაროლი", და არა „ასეთი მომხმარებელი არ არსებობს".

```
┌─ ნაბიჯი 2 ─ POST /auth/verify-reset-code  { email, code } ─────┐
│                                                                 │
│  იპოვე ბოლო გამოუყენებელი კოდი ამ მომხმარებელზე                 │
│    ვადა გავიდა?        → 400 INVALID_RESET_CODE                 │
│    attempts >= 5?      → 429 TOO_MANY_ATTEMPTS                  │
│    bcrypt.compare ❌   → attempts++ → 400 INVALID_RESET_CODE    │
│    bcrypt.compare ✅   → resetToken = jwt.sign({                │
│                             sub: userId,                        │
│                             typ: "reset",                       │
│                             rid: <კოდის ჩანაწერის id>           │
│                          }, ვადა 10 წუთი)                       │
└─────────────────────────────────────────────────────────────────┘

┌─ ნაბიჯი 3 ─ POST /auth/reset-password  { resetToken, password } ┐
│                                                                 │
│  jwt.verify(resetToken)   ❌ → 400 INVALID_RESET_TOKEN          │
│  typ === "reset"?         ❌ → 400 (access-ტოკენი არ გამოდგება) │
│  კოდის ჩანაწერს usedAt აქვს? → 400 RESET_TOKEN_USED             │
│                                                                 │
│  ტრანზაქციაში, ერთდროულად:                                      │
│    user.passwordHash = bcrypt.hash(ახალი პაროლი)                │
│    resetCode.usedAt  = ახლა                                     │
└─────────────────────────────────────────────────────────────────┘
```

### სამი დაცვის მექანიზმი

| მექანიზმი | რისგან იცავს |
|---|---|
| კოდი ბაზაში **ჰეშირებულია** | ბაზაზე წვდომით კოდების წაკითხვას |
| **5 მცდელობის ლიმიტი** | 6-ციფრიანი კოდის გამოცნობას (1 მილიონიდან) |
| `usedAt` + **ტრანზაქცია** | ერთი ტოკენით ორჯერ პაროლის შეცვლას |

**რატომ ორი ნაბიჯი (კოდი → ტოკენი → პაროლი), და არა ერთი?**
ასე კოდი მხოლოდ ერთხელ იგზავნება ქსელით. მას შემდეგ მუშაობს ხანმოკლე ტოკენი,
რომელიც კონკრეტულ კოდზეა მიბმული (`rid`) და ერთჯერადია.

---

## 7. მონაცემთა მოდელი

```prisma
model User {
  id           String   @id @default(cuid())
  name         String
  email        String   @unique        ← ორი ერთნაირი ელფოსტა შეუძლებელია
  passwordHash String                  ← bcrypt-ჰეში, არასდროს ღია პაროლი
  createdAt    DateTime @default(now())
  resetCodes   PasswordResetCode[]
}

model PasswordResetCode {
  id        String    @id @default(cuid())
  userId    String
  user      User      @relation(..., onDelete: Cascade)  ← მომხმარებელთან ერთად იშლება
  codeHash  String                     ← კოდიც ჰეშირებულია
  expiresAt DateTime                   ← 15 წუთი
  usedAt    DateTime?                  ← null = ჯერ არ გამოყენებულა
  attempts  Int       @default(0)      ← მცდელობების მრიცხველი
}
```

`@unique` ბაზის დონეზეა — ანუ დუბლიკატი შეუძლებელია მაშინაც კი, თუ ორი მოთხოვნა
ზუსტად ერთდროულად მოვა. კოდში `409 EMAIL_TAKEN` უფრო ლამაზი შეტყობინებისთვისაა.

---

## 8. შეცდომების ერთიანი ფორმატი

```json
{ "message": "ადამიანისთვის", "code": "MACHINE_CODE" }
```

ვალიდაციას (`422`) დამატებით აქვს `errors`:

```json
{
  "message": "Validation failed",
  "code": "VALIDATION_ERROR",
  "errors": { "email": "Enter a valid email address" }
}
```

**რატომ ორი ველი:**
- `message` — მომხმარებელს ვაჩვენებთ
- `code` — **კოდი წყვეტს**, რა გააკეთოს (`TOKEN_EXPIRED` → logout; `EMAIL_TAKEN` → ველზე შეცდომა)

ტექსტზე დაყრდნობა (`if (message === "...")`) მყიფეა — ტექსტი იცვლება, კოდი არა.

| code | სტატუსი | როდის |
|---|---|---|
| `VALIDATION_ERROR` | 422 | ველები არავალიდურია |
| `EMAIL_TAKEN` | 409 | ელფოსტა უკვე რეგისტრირებულია |
| `INVALID_CREDENTIALS` | 401 | არასწორი ელფოსტა ან პაროლი |
| `UNAUTHORIZED` | 401 | ტოკენი არ არის |
| `TOKEN_EXPIRED` | 401 | ტოკენს ვადა გაუვიდა |
| `INVALID_TOKEN` | 401 | ხელმოწერა არასწორია |
| `INVALID_RESET_CODE` | 400 | კოდი არასწორია ან ვადაგასულია |
| `TOO_MANY_ATTEMPTS` | 429 | 5+ არასწორი მცდელობა |
| `RESET_TOKEN_USED` | 400 | resetToken უკვე გამოყენებულია |

---

## 9. ⚠️ ერთი განზრახი ასიმეტრია

`/auth/register` — პაროლი: მინ. 8 სიმბოლო, 1 ასო, 1 ციფრი
`/auth/login` — პაროლი: **მხოლოდ სავალდებულო**

ეს შეცდომა არ არის. მომხმარებლის არსებული პაროლი შეიძლება ძველი პოლიტიკით
იყოს შექმნილი. თუ შესვლაზე სირთულეს შეამოწმებ, **ძველი მომხმარებელი საკუთარ
ანგარიშში ვეღარ შევა** — მიუხედავად იმისა, რომ პაროლი სწორია.

იგივე წესი ფრონტზეც უნდა დაიცვას.

---

## 10. როგორ ხდება დეპლოი

```
შენ:  git push origin main
        │
        ▼
GitHub: ახალი კომიტი
        │  (Render უსმენს)
        ▼
Render: 1. npm install
        2. npm run generate:pg
              → Prisma Client იქმნება Postgres-ის სქემით
        3. npm start
              → prisma db push     ცხრილები Neon-ში სინქრონდება
              → node src/server.js სერვერი ეშვება
        4. /api/health პასუხობს 200-ს → სტატუსი Live
```

`prisma db push` **იდემპოტენტურია** — თუ ცხრილები უკვე სწორია, არაფერს აკეთებს.
სქემის შეცვლისას (ახალი ველი, ახალი მოდელი) ავტომატურად ასწორებს ბაზას.

### ორი სქემის ფაილი

| ფაილი | სად გამოიყენება |
|---|---|
| `schema.prisma` | ლოკალურად — SQLite, ფაილში, ინტერნეტის გარეშე |
| `schema.postgres.prisma` | დეპლოიზე — Neon Postgres |

**მოდელები იდენტურია.** თუ ერთს შეცვლი, მეორეც შეცვალე.

---

## 11. გარემოს ცვლადები

| ცვლადი | რას აკეთებს |
|---|---|
| `DATABASE_URL` | ბაზის მისამართი. `-pooler`-იანი, რადგან Render-ის პროცესი ხშირად იხსნება/იხურება |
| `JWT_SECRET` | ტოკენების ხელმოწერის გასაღები. **გამჟღავნება = ნებისმიერ ანგარიშში შესვლა** |
| `ACCESS_TOKEN_TTL` | ტოკენის ვადა (`30m`) |
| `RESET_TOKEN_TTL` | resetToken-ის ვადა (`10m`) |
| `ARTIFICIAL_LATENCY_MS` | ხელოვნური დაყოვნება ყველა მოთხოვნაზე |
| `DEV_EXPOSE_RESET_CODE` | `true` → კოდი ბრუნდება `devCode`-ში (ელფოსტის გარეშე ტესტირებისთვის) |
| `CORS_ORIGINS` | დაშვებული დომენები. ცარიელი = ყველა |

`.env` **არასდროს** შედის git-ში. რეპოში მხოლოდ `.env.example`.

---

## 12. სასწავლო „სახელურები" ლექტორისთვის

| პარამეტრი | რას ამოწმებ სტუდენტებთან |
|---|---|
| `ARTIFICIAL_LATENCY_MS=800` | დაწერეს თუ არა loading-მდგომარეობები |
| `ACCESS_TOKEN_TTL=1m` | ამუშავებენ თუ არა ვადაგასულ სესიას (`401 TOKEN_EXPIRED`) |
| `DEV_EXPOSE_RESET_CODE=false` | კოდი მხოლოდ სერვერის ლოგში — რეალური სცენარი |
| `CORS_ORIGINS=<ერთი დომენი>` | ესმით თუ არა, რა არის CORS |

ცვლილება Render-ში: **Environment → Save** → სერვისი ავტომატურად გადაიტვირთება.
